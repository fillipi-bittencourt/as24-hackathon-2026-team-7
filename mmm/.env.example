# Environment variable fallback for API keys.
# Preferred method: copy credentials.json.example → credentials.json and fill in your keys.
# Use this file only if you prefer env vars over credentials.json.

OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
